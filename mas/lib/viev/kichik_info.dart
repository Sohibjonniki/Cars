import 'package:flutter/material.dart';

class KichikInfo extends StatelessWidget {
  const KichikInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("hjzbcjsb")),
    body: Column(children: [
      Text("nimadir bor")
    ])
    ,);
  }
}